services:
  - type: web
    name: superchat
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: superchat-data
      mountPath: /data
      sizeGB: 1
